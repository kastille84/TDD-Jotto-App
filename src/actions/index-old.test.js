//here for testing action creators
//although an argument can be made to skip testing action creators
//and only do integration tests with state
//it's a reminder not to test implementation details
//it's just here to show you how to test action creators


import {correctGuess, actionTypes}  from '.';

describe('correctGuess', () => {
  // test('returns an action with type `CORRECT_GUESS`', () => {
  //   const action = correctGuess();
  //   //.toBe will fail we need to use toEqual to compare objects
  //   expect(action).toEqual({type: actionTypes.CORRECT_GUESS})
  // });
  test('just here as a placeholder', () => {
    
  })
}); 